package educacao.firstclass;
public class Info {
	 /*
     * Programação orientada a objetos é um paradigma baseado no conceito de objetos.
     * Contém dados em forma de campos, ou atributos, e códigos em forma de procedimentos, ou métodos.
     * 
     * Enquanto a programação estruturada é voltada a procedimentos e funções definidas pelo usuário,
     * a programação orientada a objetos é voltada para conceitos como classes e objetos.
     * 
     * Classe é a estrutura ou representação que direciona a criação dos objetos de mesmo tipo.
     * Identificador é o propósito existencial aos objetos que serão criados.
     * Características (atributos) são propriedades que representam o estado do objeto.
     * Comportamentos (métodos) são ações comportamentais que um objeto dispõe.
     * Instanciar (new) é o ato de criar um objeto a partir de estrutura definida em uma classe.
     * 
     * Classes de modelo: classes que representem estrutura de domínio da aplicação (cliente, pedido)
     * Classes de serviço: classes que contém regras de negócio e validação do nosso sistema.
     * Classes de repositório: classes que contém uma integração com banco de dados.
     * Classes de controle: classes que possuem finalidade de disponibilizar alguma comunicação externa (api)
     * Classes utilitárias: classes que contém recursos comuns à toda nossa aplicação.
     */

     /* 
      * Modificador public: qualquer outra classe em qualquer outro pacote pode visualizar os recursos desta.
      * Modificador private:
      * Modificador protected: 
      */
}
